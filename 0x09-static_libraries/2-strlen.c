#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strlen - calculates the length of a string
 * @s: pointer parameter
 * Return: returns the actual length of the string
 */
int _strlen(char *s)
{
	return (strlen(s));
}
