#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strcpy - an empty function
 * @dest: parameter
 * @src: parameter
 * Return: always 0 success
 *
 */
char *_strcpy(char *dest, char *src)
{
	return (strcpy(dest, src));
}
