#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _strcat - a function
 * @dest: parameter value
 * @src: parameter value
 * Return: always 0 a success
 */
char *_strcat(char *dest, char *src)
{
	return (strcat(dest, src));
}
