#include "main.h"
#include <stdio.h>
#include <stdlib.h>
#include <ctype.h>
#include <string.h>
/**
 * _isdigit - finds a digit in alist
 * @c: parameter list
 * Return: returns a digit
 *
 */
int _isdigit(int c)
{
	return (isdigit(c));
}
