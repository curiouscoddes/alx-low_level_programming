#include "main.h"
#include <stdio.h>

/**
 * main - Check the code
 * Return: always 0 (success)
 *
 */
int main(void)
{
	int n;

	n = _strlen_recursion("Methu Kuria");
	printf("%d\n", n);
	return (0);
}
